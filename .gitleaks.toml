title = "Carbon Copy Gitleaks Config"

[allowlist]
description = "Ignore false positives"
paths = [
  # Next.js build artifacts — internal hashes, not real secrets
  '''web-app/\.next/''',
  # Lock files with high-entropy strings
  '''package-lock\.json''',
  '''yarn\.lock''',
]
# Also allowlist the specific commit where .next was accidentally tracked
commits = [
  "81e54e5c12ea13b89a3bb746d5030736b95e91ae",
]
